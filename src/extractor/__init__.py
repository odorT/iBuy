from src.extractor.distributor import Distributor, DistributorOptions

distributor = Distributor()

# options = DistributorOptions()
# options.add_argument('amazon')
# options.add_argument('tapaz')
#
# distributor_2 = Distributor(options=options)
