# from src.extractor.scraper_tapaz import Scrape_tapaz
# # from src.extractor.scraper_amazon import Scrape_amazon
# # from src.extractor.scraper_amazon import Scrape_aliexpress
#
#
# def distributor(websites, item, timeout=0.4, min_price, max_price, sort_option, currency, mode):
#     if 'tapaz' in websites:
#         api = Scrape_tapaz(item, timeout=0.4, min_price=min_price, max_price=max_price, sort_option=sort_option, currency=currency, mode=mode)
#
